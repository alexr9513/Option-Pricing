#pragma once
#include <iostream>
#include <vector>

template <typename T>
class BinaryTree
{
private:
	size_t _depth;
	std::vector<std::vector<T>> _tree;
public:
	BinaryTree(size_t depth)
};

